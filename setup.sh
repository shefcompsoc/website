#!/bin/bash

npm install -g gulp
npm install gulp gulp-autoprefixer gulp-livereload gulp-sass gulp-sourcemaps gulp-concat

echo "run 'gulp' to build and watch for changes"
echo "run 'gulp build' to only build"
